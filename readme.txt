How to install simpleimage.py:
If you’re using PyCharm, you’ll need to install Pillow:
– Open PyCharm Terminal tab and type one of the following commands
(note the capital P in Pillow):
– On a PC: py -m pip install Pillow
– On a Mac: python3 -m pip install Pillow
